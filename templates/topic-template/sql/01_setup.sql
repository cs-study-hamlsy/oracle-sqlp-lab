prompt ============================================
prompt 실습명 - 환경 준비
prompt ============================================

/*
목적
- 실습에 필요한 테이블, 인덱스, 통계정보를 현재 접속 계정 기준으로 준비한다.
- 가능하면 SQL Developer에서 추가 설치 없이 바로 실행되도록 SQL-only 방식으로 구성한다.

체크 포인트
- 실습 대상 테이블/인덱스가 정상 생성되는가
- 통계정보 수집 후 EXPLAIN PLAN이 가능한가
- 실행 직후 건수, 분포, 최소/최대 값 등 검증 결과를 바로 확인할 수 있는가

작성 가이드
- 특별한 이유가 없으면 SCOTT.EMP 같은 외부 샘플 스키마를 직접 참조하지 않는다.
- 샘플 데이터가 필요하면 인라인 데이터, dual + connect by, 또는 현재 실습 폴더 안의 독립 SQL만으로 재현 가능하게 만든다.
- 후속 파일(02_baseline.sql, 03_comparison.sql 등)이 기대하는 데이터 분포를 setup 단계에서 명확히 만든다.
- 스크립트 마지막에는 count(*), group by, min/max 등으로 데이터 준비 결과를 검증하는 조회를 넣는다.
*/
